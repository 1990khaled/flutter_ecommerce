import 'package:flutter/material.dart';

class EditUserInformation
 extends StatelessWidget {
  const EditUserInformation
  ({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar (title: const Text("EditUserInformation"),),
    );
  }
}